# Better Auth Configuration
BETTER_AUTH_SECRET=your-secret-key-change-this-in-production-min-32-chars
BETTER_AUTH_URL=http://localhost:3000
NEXT_PUBLIC_BETTER_AUTH_URL=http://localhost:3000

# Database
DATABASE_PATH=file:./tempo.db

# CORS Settings (for Expo mobile app)
# If using Expo on a physical device, replace with your computer's local IP
# Example: CORS_ORIGIN=http://192.168.1.100:8081
CORS_ORIGIN=http://localhost:8081

# Debugging Tips:
# 1. Check Next.js console logs for detailed auth request/response info
# 2. Verify EXPO_PUBLIC_AUTH_URL in your Expo app matches BETTER_AUTH_URL
# 3. For physical devices, use your computer's IP address (not localhost)
# 4. Ensure BETTER_AUTH_SECRET is at least 32 characters
